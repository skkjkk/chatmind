{
  "build": {
    "command": "cd frontend && npm install && npm run build",
    "root": "."
  },
  "start": {
    "command": "cd backend && pip install -r requirements.txt && python -m uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "root": "."
  }
}
